version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: rpa_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: dayforce_rpasupport_assistant_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  ollama:
    image: ollama/ollama:latest
    container_name: rpa_ollama
    restart: always
    volumes:
      - ollama_data:/root/.ollama

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rpa_backend
    restart: always
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/dayforce_rpasupport_assistant_db
      - AI_PROVIDER=${AI_PROVIDER:-groq}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_MODEL=${GROQ_MODEL:-qwen/qwen3.8-27b}
      - CHROMA_PERSIST_DIR=/app/vector_db
      - CHROMA_COLLECTION_NAME=dayforce_rpasupport_assistant_db
      - USE_LOCAL_AI=False
      - CHAT_MODEL=${CHAT_MODEL:-gpt-4o}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OLLAMA_BASE_URL=http://ollama:11434/v1
      - EMBEDDING_MODEL=nomic-embed-text
    depends_on:
      - db
      - ollama
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: rpa_frontend
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - backend

volumes:
  postgres_data:
  ollama_data:
